
# bdd local SQLite tracker state
.bdd/

# Legacy Beads / Dolt files
.beads/
.dolt/
*.db
.beads-credential-key
.worktrees/

# Personal Claude Code settings (per-machine, not committed)
.claude/

# Generated benchmark artifacts (bd bdd-hqo2): regenerate with `./tools/build.sh --fixture`
# and `./tools/build.sh --bench` rather than committing them.
/bin/
/dist/
/testdata/bench/
