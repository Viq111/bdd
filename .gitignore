
# Beads / Dolt files (added by bd init)
.beads/
.dolt/
*.db
.beads-credential-key
.worktrees/

# Personal Claude Code settings (per-machine, not committed)
.claude/

# Generated benchmark artifacts (bd bdd-hqo2): regenerate with `make fixture`
# and `make bench` rather than committing them.
/bin/
/dist/
/testdata/bench/
