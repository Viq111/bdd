// Package schema owns the embedded, numbered schema migrations that create
// and evolve a bdd database's logical schema, and the seed data for the
// built-in statuses and types. It is deliberately empty until the
// schema/workspace card lands.
package schema
