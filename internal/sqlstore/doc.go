// Package sqlstore implements the bdd package's storage layer over a bdd
// SQLite database: card, memory, and rune persistence, queries, and the
// audit trail. It is deliberately empty until the storage cards land.
package sqlstore
