// Package cli implements the shared plumbing for the bdd command: flag
// parsing, actor resolution, exit-code mapping, stream discipline, and JSON
// rendering conventions, plus the individual subcommands built on top of
// it. It is deliberately minimal until the CLI foundation card lands.
package cli
